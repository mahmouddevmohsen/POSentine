# ── Secrets and machine-local state — never commit ──────────────
config.json
state.json
*.lock
.env
.env.*
*.token
*.pem
*.key

# ── Working correspondence — carries live credentials ───────────
# Docs/ holds the build prompts and replies. Those have already carried
# the monitor_ro SQL password and, in ksjud7.md, the Supabase JWT secret
# and service_role key. Structural exclusion, not a habit of remembering.
Docs/

# ── Customer data — real sales, employees, and cash counts ──────
# Kept local on purpose. The GitHub Actions runner checks out this
# repo; the customer's raw POS data must never land there.
data UN/

# ── Forensic/audit reports carrying real customer machine data ──
# reports/ itself is not blanket-ignored — most of it (verification scripts,
# the Phase-1/2/3 audit reports) is intended project documentation. Only the
# specific subpaths below carry real customer identity, hostnames, SQL data
# dumps, or Telegram screenshots (found un-ignored during the 2026-08-16
# final closure audit, in a PUBLIC repo — a routine `git add -A` would have
# published them).
reports/reports from telegram/
reports/report from clint PC/
reports/session_report.md
reports/FINAL_CUSTOMER_FORENSIC_VALIDATION_REPORT.txt
reports/FINAL_MASTER_FORENSIC_REPORT.txt
reports/TOOLING_SETUP_REPORT.md

# ── Customer dashboard deliverable — real business data inside ──
# The dashboard carries the customer's shifts, cash counts, cashier
# names and product data in its fixture. The repo is PUBLIC: a routine
# `git add -A` must never publish it. It will be shipped deliberately
# (git add -f / un-ignore) as part of the Supabase integration phase.
dashboard/

# ── Built artefact — never committed ────────────────────────────
# ship/ is assembled by make_ship.py from the files in this repository.
# Committing a second copy of agent.py is precisely how the customer
# machine ends up running code that drifted from the tests months ago.
# Rebuild it, do not store it.
ship/

# ── Python ──────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
*.egg-info/

# ── Editors / OS ────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini

# ── The release artifact — built, never committed ───────────────
# `python make_ship.py --zip` produces this from ship/. It is what the
# operator downloads, and it carries MANIFEST.txt so a ZIP install is
# integrity-checked exactly like a clone. Attach it to a GitHub Release;
# committing it would be a second copy of the code, which is the whole
# thing ship/ exists to avoid.
posentine-*.zip

# ── Diagnostics collected FROM a customer machine — never commit ────
# collect_diagnostics.bat produces these, and they are unpacked here to
# read. They carry no password and no token, but they do carry the till's
# Windows username, folder layout, installed packages and ODBC drivers.
# Untracked was not enough: one `git add -A` publishes all of it.
diagnostics_*/
diagnostics_*.zip
# The same files, when a bundle is unpacked into the repository root.
readonly_proof.txt
manifest_check.txt
cloud.txt
task.xml
task_info.txt
versions.txt
odbc.txt
folder.txt
config.redacted.json
install_logs/
# Anchored to the root and named exactly: this is the bundle's own
# README.txt. The repository's readme is README.md, so there is no
# collision, and an unanchored `README.txt` could hide a real file later.
/README.txt
