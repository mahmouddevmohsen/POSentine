{
  "_comment": "Copy to config.json and fill in. config.json is gitignored and must never be committed. The agent token is minted with mint_agent_token.py; service_role must never appear on this machine.",

  "tenant_id": "00000000-0000-0000-0000-000000000000",
  "source_id": "00000000-0000-0000-0000-000000000000",

  "supabase_url": "https://<project-ref>.supabase.co",
  "supabase_anon_key": "<anon public key — admits at the API gateway>",
  "supabase_agent_token": "<from mint_agent_token.py — carries tenant_id for RLS>",

  "sql": {
    "server": "localhost\\HDSOFT",
    "database": "HD_Rest_Cashier",
    "user": "monitor_ro",
    "password": "<monitor_ro password>"
  },

  "sync_seconds": 180,
  "max_new_invoices_per_cycle": 5000,
  "rescan_every_n_cycles": 5,
  "reference_every_n_cycles": 20
}
